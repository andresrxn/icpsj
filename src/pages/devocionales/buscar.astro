---
import Search from '@/components/pages/devotionals/Search.astro'
export const prerender = false

const query = Astro.url.searchParams.get('q')
if (!query || query === '' || query.length < 2) return Astro.redirect('/404')
---

<Search />
