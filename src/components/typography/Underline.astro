---
import { cn } from '@/utils/theme'
import type { HTMLAttributes } from 'astro/types'

interface Props extends HTMLAttributes<'u'> {}
const { class: className, ...props } = Astro.props
---

<u class={cn('text-inherit text-body-inherit font-body decoration underline', className)} {...props}
  ><slot /></u
>
